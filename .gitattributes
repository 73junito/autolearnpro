# Track large binary artifacts with Git LFS where appropriate
# Patterns here should match files you want stored in Git LFS instead of history
# Next.js native modules (example)
frontend/web/node_modules/**/next-swc*.node filter=lfs diff=lfs merge=lfs -text
# ZIP archives
downloads/*.zip filter=lfs diff=lfs merge=lfs -text
# Visual Studio caches/snapshots
.vs/** filter=lfs diff=lfs merge=lfs -text
# Large generated artifacts
theme/assets/images/** filter=lfs diff=lfs merge=lfs -text
