# Perkins V & FWG Mapping CSV Export

This folder contains example mappings and a small Node script to generate a CSV export suitable for Perkins-style reporting and local review. It includes an example entry aligned to `FWG-5803` (Heavy Mobile Equipment Maintenance).

Usage:

```bash
node generate_mapping_csv.js
```

Output: `perkins_export.csv`
