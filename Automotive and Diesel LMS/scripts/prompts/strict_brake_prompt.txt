OUTPUT ONLY A SINGLE VALID JSON OBJECT with exactly these keys: written_steps (string), audio_script (string), practice_activities (array), visual_diagrams (array). Do NOT include any commentary, markdown fences, or non-JSON text.

Lesson metadata:
- course: auto-diesel
- module: brakes
- lesson_title: Brake Safety
- lesson_type: lesson
- duration_minutes: 15

Requirements:
- `written_steps`: Provide ONE markdown-formatted string (not an array). Inside that string include 6–12 numbered steps (use "1. ", "2. ", etc.), each step 1–3 sentences. After the steps include a bolded "Verification checklist" section (markdown) with 4–8 short bullet items. Example structure inside the string:
  1. Step text...
  2. Step text...
  **Verification checklist**
  - Item 1
  - Item 2
- `audio_script`: Provide a single plain-text instructor script of 200–450 words (approx. 90–180 seconds at normal speaking pace). Make it conversational, include short engagement prompts and a few brief pauses marked like "(pause)". Do NOT include numbered lists or JSON inside this string.
- `practice_activities`: Provide an array of 1–2 scenario objects. Each object must include `type`, `title`, `description`, and `questions` (array).
- `visual_diagrams`: Provide an array of 0–2 objects; each object should include `type`, `description`, and `labels` (array).
- Output constraints: All values must be JSON-serializable. `written_steps` and `audio_script` must be strings. Keep language technical and safety-focused. No PII. No external links.

Return exactly one valid JSON object and nothing else.
