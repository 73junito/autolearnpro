# Student Dashboard

This folder contains the student-facing dashboard UI for the catalog template.

Place UI components under `components/`, styles under `styles/`, and utility scripts under `scripts/`. Add `layout.tsx` as the main layout wrapper and `index.tsx` as the entry.
