// Placeholder layout for student dashboard
export default function Layout({ children }: { children: React.ReactNode }) {
  return (
    <div className="student-dashboard-layout">
      {children}
    </div>
  )
}
