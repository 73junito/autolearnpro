# Use NPM token from GitHub Actions secrets for private package installs
# Do NOT commit actual tokens to the repository. Set NPM_TOKEN in repository secrets.
//registry.npmjs.org/:_authToken=${NPM_TOKEN}

# Optional: always-auth so registry auth is always sent
always-auth=true
